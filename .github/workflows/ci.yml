name: day35

on: push

env:
  NAME: Siddu
  CITY: Hyderabad

jobs:
  build:
   runs-on: ubuntu-latest

   steps:
   - name: Step 1
     run: echo "Hello"

   - name: Step 2
     if: true
     run: echo "World"
