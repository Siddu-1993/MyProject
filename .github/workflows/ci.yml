name: Upload Artifact

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: mkdir output
      - run: echo "Hello Siddu" > output/result.txt

      - uses: actions/upload-artifact@v4
        with:
          name: my-files
          path: output/
